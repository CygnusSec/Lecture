% Generated from source page 98
\begin{frame}[t]{Msfconsole - Starting. (continued)}
  \begin{itemize}
    \setlength{\itemsep}{0.12cm}
    \item \BodyText{Initialise the MSF database named 'msf' in PostgreSQL}
      \item \CommandText{sudo msfdb init}
        \item \BodyText{only do it once when msfconsole is to run for the first time}
    \item \BodyText{Launch msfconsole (will take a little while)}
      \item \CommandText{sudo msfconsole}
  \end{itemize}
\end{frame}
