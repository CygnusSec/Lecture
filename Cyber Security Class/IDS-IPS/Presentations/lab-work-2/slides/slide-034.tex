% Generated from source page 20
\begin{frame}[t]{nmap: Host Discovery - Options. (continued)}
  \begin{itemize}
    \setlength{\itemsep}{0.12cm}
    \item \BodyText{Only do TCP ACK (-PA) : port 80 will be probed by default; can change by -PA<port number>.}
      \item \CommandText{sudo nmap -PA 172.16.1.10 \# using port 80}
      \item \CommandText{sudo nmap -PA443 172.16.1.10 \# using port 443}
  \end{itemize}
\end{frame}
