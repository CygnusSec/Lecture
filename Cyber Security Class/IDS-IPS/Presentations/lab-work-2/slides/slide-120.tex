% Generated from source page 59
\begin{frame}[t]{GVM - Create A Target. (continued)}
  \begin{itemize}
    \setlength{\itemsep}{0.12cm}
      \item \BodyText{Enter the IP address of the host (typically, a target consists of one host)}
      \item \BodyText{Select the Port List to scan: use the default}
      \item \BodyText{Select the Alive Test (i.e.,Host Discovery) method: use the default}
  \end{itemize}
\end{frame}
