% Generated from source page 28
\begin{frame}[t]{nmap: Service detection. (continued)}
  \begin{itemize}
    \setlength{\itemsep}{0.12cm}
    \item \BodyText{Based on this database, nmap tries to determine the service protocol (e.g. FTP, SSH, Telnet, HTTP), the application name (e.g. vsftpd, Apache httpd, Solaris telnetd), the version number, etc. of a service.}
  \end{itemize}
\end{frame}
