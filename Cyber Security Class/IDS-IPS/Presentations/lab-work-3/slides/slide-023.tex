% Source PDF page 23 - reconstructed table
\begin{frame}{Common Extended Match Criteria}
\centering\fontsize{10.7}{13}\selectfont
\renewcommand{\arraystretch}{1.12}
\begin{tabular}{|Y{5.0cm}|Y{8.0cm}|}\hline
\rowcolor{MethodColor!80}\color{white}\bfseries Switch&\color{white}\bfseries Description\\\hline
\textcolor{CommandGreen}{\ttfamily\bfseries -m multiport -{}-port <port, port>}&A variety of TCP/UDP source ports separated by commas. Unlike when -m isn't used, they do not have to be within a range.\\\hline
\textcolor{CommandGreen}{\ttfamily\bfseries -m multiport -{}-dport <port, port>}&A variety of TCP/UDP destination ports separated by commas. Unlike when -m isn't used, they do not have to be within a range.\\\hline
\textcolor{CommandGreen}{\ttfamily\bfseries -m multiport -{}-ports <port, port>}&A variety of TCP/UDP ports separated by commas. Source and destination ports are assumed to be the same and they do not have to be within a range.\\\hline
\end{tabular}
\vspace{0.35cm}
{\color{WarningColor}\ttfamily\fontsize{12}{15}\selectfont iptables -A FORWARD -s 0/0 -i eth0 -d 192.168.1.58 -o eth1 -p TCP -{}-sport 1024:65535 -m multiport -{}-dport 80,443 -j ACCEPT\par}
\end{frame}
